name: Deploy to Cloud Server

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json

    - name: Build backend
      run: |
        cd backend
        npm ci
        npm run build

    - name: Prepare deployment package
      run: |
        echo "📦 准备部署包..."
        mkdir -p deploy-package/backend
        mkdir -p deploy-package/frontend

        # 复制后端必要文件
        cp -r backend/dist deploy-package/backend/
        cp backend/package.json deploy-package/backend/
        cp backend/package-lock.json deploy-package/backend/
        cp backend/.env.example deploy-package/backend/
        cp backend/ecosystem.config.cjs deploy-package/backend/

        # 复制前端文件（排除 node_modules）
        rsync -av --exclude='node_modules' --exclude='.git' --exclude='*.log' frontend/ deploy-package/frontend/

        echo "✅ 部署包准备完成"
        du -sh deploy-package/*

        # 创建压缩包（备用方案）
        echo "📦 创建压缩包..."
        tar -czf deploy-package.tar.gz -C deploy-package .
        ls -lh deploy-package.tar.gz

    - name: Deploy to server
      uses: appleboy/scp-action@v0.1.7
      continue-on-error: true
      id: scp_deploy
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: 22
        source: "deploy-package.tar.gz"
        target: "/tmp/"
        timeout: 15m
        command_timeout: 15m
        overwrite: true
        strip_components: 0
        rm: false
        use_insecure_cipher: true
      env:
        DEBUG: true
    
    - name: Retry deploy if failed (attempt 2)
      if: steps.scp_deploy.outcome != 'success'
      uses: appleboy/scp-action@v0.1.7
      continue-on-error: true
      id: scp_deploy_retry
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: 22
        source: "deploy-package.tar.gz"
        target: "/tmp/"
        timeout: 15m
        command_timeout: 15m
        overwrite: true
        use_insecure_cipher: true
    
    - name: Extract package on server
      if: steps.scp_deploy.outcome == 'success' || steps.scp_deploy_retry.outcome == 'success'
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: 22
        script: |
          echo "📦 解压部署包..."
          rm -rf /tmp/hikingSocialApp
          mkdir -p /tmp/hikingSocialApp
          tar -xzf /tmp/deploy-package.tar.gz -C /tmp/hikingSocialApp/
          echo "✅ 解压完成"
          ls -lah /tmp/hikingSocialApp/

    - name: Fallback - Manual upload instruction
      if: steps.scp_deploy.outcome != 'success' && steps.scp_deploy_retry.outcome != 'success'
      run: |
        echo "❌ 自动部署失败"
        echo "建议手动部署："
        echo "1. scp deploy-package.tar.gz user@server:/tmp/"
        echo "2. ssh user@server"
        echo "3. cd /tmp && tar -xzf deploy-package.tar.gz"
        exit 1

    - name: Verify upload
      if: steps.scp_deploy.outcome == 'success' || steps.scp_deploy_retry.outcome == 'success'
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: 22
        script: |
          echo "✅ 验证上传文件..."
          ls -lah /tmp/hikingSocialApp/
          echo "后端文件:"
          ls -lah /tmp/hikingSocialApp/backend/ || echo "后端目录不存在"
          echo "前端文件:"
          ls -lah /tmp/hikingSocialApp/frontend/ || echo "前端目录不存在"

    - name: Execute deployment commands
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: 22
        script: |
          set -e
          set -o pipefail

          echo "=========================================="
          echo "🚀 开始部署..."
          echo "=========================================="

          PROJECT_DIR="/var/www/hikingSocialApp"
          TEMP_DIR="/tmp/hikingSocialApp"

          # 确保项目目录存在
          mkdir -p "$PROJECT_DIR" || (echo "❌ 无法创建项目目录" && exit 1)

          # 备份现有环境变量文件
          if [ -f "$PROJECT_DIR/backend/.env" ]; then
            echo "📦 备份环境变量文件..."
            cp "$PROJECT_DIR/backend/.env" "/tmp/backend.env.backup"
          fi

          # 同步代码（从临时目录到项目目录）
          echo "📥 同步代码..."
          rsync -av --delete "$TEMP_DIR/backend/" "$PROJECT_DIR/backend/" || (echo "❌ 后端同步失败" && exit 1)
          rsync -av --delete "$TEMP_DIR/frontend/" "$PROJECT_DIR/frontend/" || (echo "❌ 前端同步失败" && exit 1)

          # 恢复环境变量文件
          if [ -f "/tmp/backend.env.backup" ]; then
            cp "/tmp/backend.env.backup" "$PROJECT_DIR/backend/.env"
            rm "/tmp/backend.env.backup"
          fi

          # 清理临时目录
          rm -rf "$TEMP_DIR"

          # 后端部署
          echo "=========================================="
          echo "🔧 部署后端..."
          echo "=========================================="
          cd "$PROJECT_DIR/backend"

          echo "📦 安装生产依赖..."
          npm ci --production || npm install --production || (echo "❌ npm install 失败" && exit 1)

          # 重启后端服务
          echo "🔄 重启后端服务..."
          if pm2 list | grep -q hiking-app-backend; then
            pm2 restart ecosystem.config.cjs --only hiking-app-backend --env production || (echo "❌ 重启失败" && exit 1)
          else
            pm2 start ecosystem.config.cjs --env production || (echo "❌ 启动失败" && exit 1)
          fi
          pm2 save

          # 前端部署
          echo "=========================================="
          echo "🎨 部署前端..."
          echo "=========================================="
          cd "$PROJECT_DIR/frontend"

          if [ ! -d node_modules ]; then
            echo "📦 安装依赖..."
            npm install || (echo "❌ 前端依赖安装失败" && exit 1)
          fi

          echo "🔨 构建项目..."
          npm run build || (echo "❌ 前端构建失败" && exit 1)

          # 重启 Nginx
          echo "🔄 重启 Nginx..."
          sudo systemctl reload nginx || true

          echo "=========================================="
          echo "✅ 部署完成！"
          echo "=========================================="

          # 显示服务状态
          pm2 list

    - name: Health check
      run: |
        sleep 8
        echo "🏥 健康检查..."

        # 检查后端 API
        for i in {1..5}; do
          echo "尝试连接后端 ($i/5)..."
          if curl -s -f http://115.190.252.62:3000/api/v1/activities?page=1 -H "Accept: application/json" > /dev/null; then
            echo "✅ 后端 API 健康检查通过"
            break
          else
            if [ $i -lt 5 ]; then
              echo "⏳ 等待中..."
              sleep 3
            else
              echo "⚠️ 后端可能还在启动，继续部署"
            fi
          fi
        done

        # 检查前端
        echo "检查前端..."
        if curl -s -f http://115.190.252.62/ > /dev/null; then
          echo "✅ 前端可访问"
        else
          echo "⚠️ 前端可能还在启动"
        fi

    - name: Notify status
      if: always()
      run: |
        if [ "${{ job.status }}" == "success" ]; then
          echo "🎉 部署成功！"
          echo "✅ 前端: http://115.190.252.62"
          echo "✅ 后端 API: http://115.190.252.62:3000/api/v1"
        else
          echo "❌ 部署失败"
          echo "请检查上面的错误日志"
        fi

    - name: Notify on failure
      if: failure()
      run: |
        echo "❌ 部署失败！"
        echo "失败的步骤状态: ${{ job.status }}"
